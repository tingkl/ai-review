refactor: case format Markdown to structured text + 27 test cases

format_cases_for_prompt() changes:
- Remove ###, ```, checkbox Markdown symbols
- Use [caseN|title|level] format instead of Markdown headings
- Use X/check marks for bad/good code examples
- Save ~30-40% tokens per case

Original case files still Markdown (user-friendly).
Auto-convert to structured text before sending to AI.

Add test_case_loader.py with 27 tests covering:
- Empty list / full case / missing optional fields
- Severity as int vs string / multiple examples
- Multiline content / special characters in code
- Empty label/code/question / Markdown symbols absent
- Compression ratio verification
